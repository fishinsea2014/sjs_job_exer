Deploying tips:
1. Install node.js
2. Install gulp by "npm i -g gulp"
3. Run 'json-server src/data/db.json' to start json server
4. Run 'bower install' to install dependencies for running
5. Run "gulp" to start the project.
6. View "http://localhost:3003/" in browser.

Directory structure:
1. "src" is source codes.
2. "build" is codes used to debug program.
3. "dist" is compressed codes used to deploy the project.
